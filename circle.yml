dependencies:
  pre:
    - ./misc/build-linux.sh --deps

compile:
  override:
    - ./misc/build-linux.sh --build; ./misc/build-linux.sh --install
test:
  override:
    - echo "TODO: Write tests then run them here."
