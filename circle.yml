dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install build-essentials valac libgtk-3-dev libjson-glib-dev libsoup2.4-dev

compile:
  override:
    - mkdir build/ && cd build/ ; cmake .. -DCMAKE_INSTALL_PREFIX=/usr && make && sudo make install
test:
  override:
    - echo TODO
