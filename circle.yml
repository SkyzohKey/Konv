dependencies:
  pre:
    - sudo add-apt-repository ppa:vala-team/ppa; sudo apt-get update; sudo apt-get install build-essential vala-0.30 libgtk-3-dev libjson-glib-dev libsoup2.4-dev

compile:
  override:
    - mkdir build/ && cd build/ ; cmake .. -DCMAKE_INSTALL_PREFIX=/usr && make
test:
  override:
    - echo TODO
